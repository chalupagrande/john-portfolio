{
  "name": "@john/main",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/cms dev",
    "web-dev": "yarn --cwd packages/web-app dev",
    "build": "yarn --cwd packages/cms build",
    "start": "yarn --cwd packages/cms start"
  }
}